services:
  - type: web
    name: gcash-tracker-backend
    env: node
    plan: free
    buildCommand: |
      npm install
      npm run build
    startCommand: npm run start:prod
    envVars:
      - key: ENV
        value: production
